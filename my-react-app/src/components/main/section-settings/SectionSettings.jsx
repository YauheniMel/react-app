import style from './SectionSettings.module.scss';

export default function SectionSettings() {
  return (
    <section className={`section_wrap ${style.section}`}>
      <h2>setting</h2>
    </section>
  );
}
